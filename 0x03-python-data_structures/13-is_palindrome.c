#include <stdio.h>
#include "lists.h"

/**
 * is_palindrome - checks if a linked list is a palindrome
 * @head: the linked list
 *
 * Return: 0 if it is a palindrome otherwise 1
 */

int is_palindrome(listint_t **head)
{
	if (*head == NULL)
		return (0);
	return (1);
}
